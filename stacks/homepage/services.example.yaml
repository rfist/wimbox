# Services not auto-discovered via Docker labels.
# Use this for host-network services (no Traefik route) or external URLs.
# Docker-managed services are discovered automatically via homepage.* labels.

- Media:
    - Plex:
        href: http://YOUR_PI_IP:32400/web
        description: Media server
        icon: plex

- Files:
    - Syncthing:
        href: http://YOUR_PI_IP:8384
        description: File synchronization
        icon: syncthing
